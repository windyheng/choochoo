# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Env / secrets
.env

# Model artifacts — never commit these
checkpoints/
*.pt
*.pth
*.ckpt

# Notebook checkpoints
.ipynb_checkpoints/

# Raw dataset download archives — kept locally only, extracted images are committed instead
data/raw/**/*.zip

# OS / editor
.DS_Store
.vscode/
.idea/

# results/ (robustness table, error-analysis thumbnails) is a required
# deliverable — keep it committed, do NOT gitignore it.

# Precomputed CLIP embedding cache - regenerate with data/cache_clip_embeddings.py
data/cache/clip_embeddings/
